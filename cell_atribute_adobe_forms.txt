MUDAR UM ATRIBUTO DE CAMPO A PARTIR DE VALIDAÇÃO NO ADOBE FORMS:

tenha mais um campo na tabela do tipo flag para quando quiser mudar.

no adobe forms, selecione o campo e va em script editor

faça a validação em javascript, exemplo:

Quando um material está em uma tabela de validação, quero pintar a cédula com cor diferente.

- Marco o campo flag(FLAG_COLOR) da tabela interna.

no adobe forms, script editor, faço a seguinte validação:

if (this.parent.FLAG_COLOR.rawValue === "X") {
    this.font.fill.color.value = "255,0,0";
}

ISSO MUDARA O CAMPO DESEJADO PARA VERMELHO